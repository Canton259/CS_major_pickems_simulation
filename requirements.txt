# Runtime/development dependencies for optional tools.
# Recommended Python version: Python >= 3.10
#
# The simulator itself mainly uses the standard library.
# NumPy/Numba support greedy exhaustive search acceleration.
# Playwright is only required for `python backtest.py fetch`.

numpy>=1.24
numba>=0.57
playwright>=1.44
